<%# add navigation links to this file %>

<% if user_signed_in? %>
<li class="dropdown">
	<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.username%> <span class="caret"></span></a>
	<ul class="dropdown-menu">
		<!-- <li><%= link_to 'Kommentare anzeigen', comments_path%> -->
		<li>
			<%= link_to 'Neues Bild hochladen', new_art_path%>
		</li>
		<li>
			<%= link_to 'Bilder verwalten', arts_path%>
		</li>
		<% if current_user.has_role? :admin%>
		<li>
			<%= link_to 'Neue Sammlung anlegen', new_gallery_path%>
		</li>
		<%end%>
		<li>
			<%= link_to 'Sammlungen verwalten', galleries_all_path%>
		</li>
		<li>
			<%= link_to 'Nutzer editierne', user_path(current_user) %>
		</li>
		<li role="separator" class="divider"></li>
		<li>
			<%= link_to 'Ausloggen', destroy_user_session_path, :method=>'delete' %>
		</li>
	</ul>
</li>
<% else %>

<li>
	<!--<%= link_to t('.sign_up', :default => "Sign up"), new_user_registration_path %></li>
	<a href="" data-toggle="modal" data-target="#modal-signup"><font color="#FFFFFF">Registieren</font></a>-->
	<%= link_to "Registieren", '',:data => {:toggle=>"modal",:target=>"#modal-signup"} %>
	<% if can? :update, User %>
<li>
	<%= link_to "Nutzer",users_path	%>

</li>
<%end%>
<% end %>